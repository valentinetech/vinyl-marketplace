import { theme } from 'common/styles/theme';
import styled from 'styled-components';

export const CountdownTitle = styled.h6`
	text-align: center;
	margin-top: 20px;
`;
export const CountdownComponent = styled.p`
	text-align: center;
	font-size: 20px;
	color: ${theme.colors.brand};
`;
